import React from "react";

import Title from "../../reusable/Title";

const Cuisine = () => {
  return (
    <div className="cuisine">
      <Title text = {"Cuisines"} />
    </div>
  );
};

export default Cuisine;
