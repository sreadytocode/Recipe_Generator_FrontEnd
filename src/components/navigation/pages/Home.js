import React from "react";

import Title from "../../reusable/Title";

const Home = () => {
  return (
    <div className="home">
      <Title text = {"Home"} />
    </div>
  );
};

export default Home;
