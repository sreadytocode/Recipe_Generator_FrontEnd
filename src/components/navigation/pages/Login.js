import React from "react";

import Title from "../../reusable/Title";

const Login = () => {
  return (
    <div className="login">
      <Title text = {"login"} />
    </div>
  );
};

export default Login;
